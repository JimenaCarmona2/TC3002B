programa fibonacci;
entero fib ( n : entero ) {
  vars
    r : entero;
  {
    r = n;
    si ( n > 1 ) {
      r = fib ( n - 1 ) + fib ( n - 2 );
    };
    regresa r;
  }
};
inicio
{
  escribe ( "Fibonacci fib(0)..fib(7):" );
  escribe ( fib ( 10 ) );
  escribe ( fib ( 11 ) );
  escribe ( fib ( 12 ) );
  escribe ( fib ( 13 ) );
  escribe ( fib ( 14 ) );
  escribe ( fib ( 15 ) );
  escribe ( fib ( 16 ) );
  escribe ( fib ( 17 ) );
}
fin
